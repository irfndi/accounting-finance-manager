import { describe, it, expect } from 'vitest';

// Example unit test - tests individual functions/classes in isolation
describe('Unit Tests Example', () => {
  it('should demonstrate unit testing pattern', () => {
    // Unit tests focus on testing individual functions or classes
    // They should be fast and isolated
    expect(true).toBe(true);
  });
});